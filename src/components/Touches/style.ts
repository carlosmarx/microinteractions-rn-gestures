import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: "center",
        alignItems: "center"
    },
    box: {
        borderRadius: 12,
        padding: 12,
        justifyContent: "center",
        alignItems: "center"
    },
    text: {
        color: "#fff",
        textAlign: "center"
    }
})