import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: "center",
        alignItems: "center"
    },
    box: {
        borderRadius: 12,
        width: 100,
        height: 100,
        backgroundColor: "#0bb17a",
        justifyContent: "center",
        alignItems: "center"
    },
    text: {
        color: "#fff"
    }
})